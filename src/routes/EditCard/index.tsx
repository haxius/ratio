import React, { FC } from "react";
import { Route } from "../../context/Router/models";

const EditCard: FC = () => <>Edit Card</>;

export default {
  name: "edit-card",
  component: EditCard,
} as Route;
